[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-version"
version = "0.1.0"
description = "Tile versioning — commit, branch, merge, rollback"
license = {text = "MIT"}
requires-python = ">=3.8"
